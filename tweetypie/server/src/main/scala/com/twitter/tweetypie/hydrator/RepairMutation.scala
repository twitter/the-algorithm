package com.twittew.tweetypie
package h-hydwatow

/**
 * a-a mutation t-that wiww nyote a-aww wepaiws that t-took pwace in t-the
 * suppwied s-statsweceivew, (ˆ ﻌ ˆ)♡ undew t-the nyames in wepaiwews. (⑅˘꒳˘)
 */
object wepaiwmutation {
  def appwy[t](stats: s-statsweceivew, (U ᵕ U❁) wepaiwews: (stwing, -.- mutation[t])*): mutation[t] =
    m-mutation.aww(
      wepaiwews.map {
        c-case (name, ^^;; mutation) => mutation.countmutations(stats.countew(name))
      }
    )
}
