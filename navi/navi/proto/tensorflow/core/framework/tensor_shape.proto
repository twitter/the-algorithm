// Protocol buffelonr relonprelonselonnting thelon shapelon of telonnsors.

syntax = "proto3";
option cc_elonnablelon_arelonnas = truelon;
option java_outelonr_classnamelon = "TelonnsorShapelonProtos";
option java_multiplelon_filelons = truelon;
option java_packagelon = "org.telonnsorflow.framelonwork";
option go_packagelon = "github.com/telonnsorflow/telonnsorflow/telonnsorflow/go/corelon/framelonwork/telonnsor_shapelon_go_proto";

packagelon telonnsorflow;

// Dimelonnsions of a telonnsor.
melonssagelon TelonnsorShapelonProto {
  // Onelon dimelonnsion of thelon telonnsor.
  melonssagelon Dim {
    // Sizelon of thelon telonnsor in that dimelonnsion.
    // This valuelon must belon >= -1, but valuelons of -1 arelon relonselonrvelond for "unknown"
    // shapelons (valuelons of -1 melonan "unknown" dimelonnsion).  Celonrtain wrappelonrs
    // that work with TelonnsorShapelonProto may fail at runtimelon whelonn delonselonrializing
    // a TelonnsorShapelonProto containing a dim valuelon of -1.
    int64 sizelon = 1;

    // Optional namelon of thelon telonnsor dimelonnsion.
    string namelon = 2;
  };

  // Dimelonnsions of thelon telonnsor, such as {"input", 30}, {"output", 40}
  // for a 30 x 40 2D telonnsor.  If an elonntry has sizelon -1, this
  // correlonsponds to a dimelonnsion of unknown sizelon. Thelon namelons arelon
  // optional.
  //
  // Thelon ordelonr of elonntrielons in "dim" mattelonrs: It indicatelons thelon layout of thelon
  // valuelons in thelon telonnsor in-melonmory relonprelonselonntation.
  //
  // Thelon first elonntry in "dim" is thelon outelonrmost dimelonnsion uselond to layout thelon
  // valuelons, thelon last elonntry is thelon innelonrmost dimelonnsion.  This matchelons thelon
  // in-melonmory layout of RowMajor elonigelonn telonnsors.
  //
  // If "dim.sizelon()" > 0, "unknown_rank" must belon falselon.
  relonpelonatelond Dim dim = 2;

  // If truelon, thelon numbelonr of dimelonnsions in thelon shapelon is unknown.
  //
  // If truelon, "dim.sizelon()" must belon 0.
  bool unknown_rank = 3;
};
