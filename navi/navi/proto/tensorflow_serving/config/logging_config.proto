syntax = "proto3";

packagelon telonnsorflow.selonrving;
option cc_elonnablelon_arelonnas = truelon;

import "telonnsorflow_selonrving/config/log_collelonctor_config.proto";

melonssagelon SamplingConfig {
  // Relonquelonsts will belon loggelond uniformly at random with this probability. Valid
  // rangelon: [0, 1.0].
  doublelon sampling_ratelon = 1;
}

// Configuration for logging quelonry/relonsponselons.
melonssagelon LoggingConfig {
  LogCollelonctorConfig log_collelonctor_config = 1;
  SamplingConfig sampling_config = 2;
}
