<% content_for :message do %>
  <p>We have found some invalid sign-ups in your challenge <%= style_link(@collection.title, collection_url(@collection)) %>. Potential matches cannot be generated until these are cleaned up.</p>

  <p>Invalid sign-ups are often duplicates, or don't meet the requirements you've set for your challenge. Unfortunately, there is no automatic way to fix them, so you will have to manually edit or delete them. For more details, see <%= style_link("the challenge matching help", root_url + "help/challenge_matching.html#invalid_signups") %>.</p>

  <p>Here are the invalid sign-ups:
    <ul>
    <% @invalid_signups.each do |signup_id| %>
      <li><%= style_link("Sign-up #{signup_id}", collection_signup_url(@collection, signup_id)) %></li>
    <% end %>
    </ul>
  </p>

  <% content_for :sent_at do %><%= l(Time.now) %><% end %>
<% end %>
