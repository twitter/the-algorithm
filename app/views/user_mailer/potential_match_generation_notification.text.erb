<% content_for :message do %>
We have finished generating potential assignments for your challenge collection "<%= @collection.title %>" (<%= collection_url(@collection) %>).

You can see the potential matches and set assignments on the challenge's Matching page:
<%= collection_potential_matches_url(@collection) %>.<% end %><% content_for :sent_at do %><%= l(Time.now) %><% end %>
